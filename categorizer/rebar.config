{erl_opts, [debug_info]}.
{ct_opts, [{dir, "tests"}]}.

{deps, []}.

{alias,[
    {build,[compile]}
    ,{check,[compile,edoc,dialyzer,eunit,ct]}
]}.

{shell, [
    {config, "config/sys.config"},
    {apps, [categorizer]}
]}.
